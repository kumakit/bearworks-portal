# Issue #378 時別入力基盤の実装記録

2026-09-14。Issue全体は進行中。

## 成果

- Portalに分析設計r1を作成し、Appsにも同文書を保存。主分析35完結冬候補、短期湿度2025年1〜2月、品質8主分析・5感度分析、降水0.5mm、イベント分離6時間・感度12時間、推測統計と直接検証不能の扱いを固定。
- Appsの最新main `08c8e3b` から `codex/issue-378-hachioji-snow` を作成。既存の日別処理は未変更。
- 公式CSV見本を取得。2024-12-22 1時〜2024-12-25 0時JST、八王子・東京、6項目、72時間・864レコード、CP932、8034 bytes。
- SHA-256: `7c06b1a9835886ff09817cf943e7f20455eb28ae28209e82bb5928c2e014a682`。元URL、POST条件、取得時刻をmanifestに保存。
- Apps `streamlit/hachioji_snow/` に日時・品質モデル、CSV parser、同時刻比較、SHA/byte検証、期待行カバレッジ検証、公式fixtureとテストを追加。

## 実データで確認したこと

八王子2024年12月22日1時の気温は品質1（欠測）、湿度は品質0（非観測）。23日17時の湿度44%、同時刻の降水は品質4。東京の現象なし降水は数値0と現象なしフラグ1。東京の天気は正時でも未観測の時刻があり、空欄を雨や雪に補完しない。

これらは可用性と入力契約の確認であり、雪仮説の結論ではない。

## 取得方法と失敗の扱い

ブラウザの公式画面で地点・項目・期間・オプションを指定し、表示表を確認。ダウンロード完了通知は得られたがローカル保存パスが返らず、公開スクリプトが送る公式フォームと同じPOST条件を用いて保存した。

最初のPOSTはHTMLエラーだったためCSVとして扱わず `jma-csv-attempt-error.html` に保持。日時配列を文字列、時間指定を1〜24に合わせた再取得で実CSVを確認。HTMLエラーをparserが拒否するテストも追加。フォームは公開APIとしての安定性を保証しないため、本取得時にもContent-Type・ヘッダー・行数・ハッシュを検証する。

## レビューと検証

Lunaは日別parser再利用境界と新規時別入力処理を読み取り専用レビュー。司令塔が直接確認し、現象なしフラグの適用範囲、型境界、品質とnull、正規化後の重複テストを追加した。期待列集合・疎な天気の行欠落は別のcoverage層で検証する。

Python実行は同梱runtimeを使用（通常PATHのpythonは見つからなかった）。`-m unittest discover -s hachioji_snow/tests -v` は13件成功。UTF-8/CP932、うるう日・年末24時、欠測・非観測・品質4/5/8、現象なし、重複、HTML・列ずれ・行欠落、改変を検証した。

## 残る工程

長期CSVの本取得、snapshot間の均質区間照合、イベント集計、H1〜H4評価、独立検算、bundle・図表・雪記事・学習導線、本番向けビルドとUI検証。新規テスト成功を記事全体の完成や公開承認とは扱わない。push・公開は未実施。
